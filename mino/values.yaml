# values.yaml
mode: standalone

rootUser: minioadmin
rootPassword: minioadmin123

persistence:
  enabled: false  # good for local dev/test only

resources:
  requests:
    memory: 512Mi
    cpu: 250m
  limits:
    memory: 1Gi
    cpu: 500m

service:
  type: ClusterIP
  port: 9000

consoleService:
  type: ClusterIP
  port: 9001
