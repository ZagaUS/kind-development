apiVersion: v1
kind: Service
metadata:
  name: iceberg-catalog-ui
spec:
  selector:
    app: iceberg-catalog-ui
  ports:
  - protocol: TCP
    port: 8080
    targetPort: 8080
  type: ClusterIP # Or LoadBalancer if needed in cloud
